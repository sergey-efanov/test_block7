// словарь для перевода на другие языки
// const dictionary = {
//   'А что получится?': {
//     'en': 'And what happens?',
//   },
// };

// const ruDictionary = {};

// function translate(selector, lang) {
//   const elements = document.querySelectorAll(selector);

//   for (let i = 0; i < elements.length; i++) {
//     let element = elements[i];
//     let originalText = element.textContent.trim();

//     if (dictionary[originalText] && dictionary[originalText][lang]) {
//       element.textContent = dictionary[originalText][lang];
//     }
//   }
// }
